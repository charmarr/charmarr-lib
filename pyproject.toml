[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "core",
    "vpn",
    "testing",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=4.0",
    "ruff>=0.1",
    "mypy>=1.8",
    "tox>=4.0",
]
